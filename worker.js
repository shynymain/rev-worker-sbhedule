const headers={"content-type":"application/json;charset=utf-8","access-control-allow-origin":"*","access-control-allow-methods":"GET,POST,OPTIONS","access-control-allow-headers":"content-type"};
const VENUES=["東京","京都","新潟","中山","阪神","中京","札幌","函館","福島","小倉"];
function json(data){return new Response(JSON.stringify(data),{headers})}
function pad(n){return String(n).padStart(2,"0")}
function ymdSlash(d){return `${d.getFullYear()}/${pad(d.getMonth()+1)}/${pad(d.getDate())}`}
function ymdCompact(s){return String(s).replace(/\D/g,"")}
function normalizeDate(s){if(!s)return"";const m=String(s).match(/(\d{4})\D?(\d{1,2})\D?(\d{1,2})/);if(!m)return"";return `${m[1]}/${pad(m[2])}/${pad(m[3])}`}
function isGradedRace(r={}){const text=[r.grade,r.raceName,r.condition].join(" ");return /G1|G2|G3|Ｇ１|Ｇ２|Ｇ３|GⅠ|GⅡ|GⅢ|重賞/i.test(text)}
function isExcludedRace(r={}){const text=[r.raceName,r.grade,r.condition,r.age].join(" ");if(/未勝利|新馬/.test(text))return true;if(/2歳|２歳/.test(text)&&!isGradedRace(r))return true;return false}
function filterTargetRaces(list){return list.filter(x=>!isExcludedRace(x.race||x))}
function thisWeekend(){const now=new Date();const day=now.getUTCDay();const diffSat=(6-day+7)%7;const sat=new Date(now);sat.setUTCDate(now.getUTCDate()+diffSat);const sun=new Date(sat);sun.setUTCDate(sat.getUTCDate()+1);return [ymdSlash(sat),ymdSlash(sun)]}
function stripTags(s){return String(s||"").replace(/<script[\s\S]*?<\/script>/gi,"").replace(/<style[\s\S]*?<\/style>/gi,"").replace(/<[^>]+>/g," ").replace(/&nbsp;|&#160;/g," ").replace(/&amp;/g,"&").replace(/\s+/g," ").trim()}
async function fetchText(url){const res=await fetch(url,{headers:{"user-agent":"Mozilla/5.0 RevRaceBot"}});if(!res.ok)throw new Error(`${res.status} ${url}`);const buf=await res.arrayBuffer();let txt=new TextDecoder("utf-8").decode(buf);if((txt.match(/\uFFFD/g)||[]).length>20){try{txt=new TextDecoder("shift_jis").decode(buf)}catch(e){}}return txt}
async function calendarRaces(date){const compact=ymdCompact(date);const y=compact.slice(0,4),m=String(Number(compact.slice(4,6)));const url=`https://www.jra.go.jp/keiba/calendar${y}/${y}/${m}/${compact.slice(4)}.html`;const html=await fetchText(url);const text=stripTags(html);const races=[];for(const place of VENUES){if(!text.includes(place))continue;const placeStart=text.indexOf(place);const nextStarts=VENUES.filter(v=>v!==place).map(v=>text.indexOf(v,placeStart+place.length)).filter(i=>i>placeStart);const end=nextStarts.length?Math.min(...nextStarts):text.length;const chunk=text.slice(placeStart,end);for(let no=1;no<=12;no++){const candidates=[`${no}レース`,`${no}R`,`${no}Ｒ`];let idx=-1;for(const c of candidates){idx=chunk.indexOf(c);if(idx>=0)break}if(idx<0)continue;let nextIdx=chunk.length;for(let n=no+1;n<=12;n++){for(const c of [`${n}レース`,`${n}R`,`${n}Ｒ`]){const t=chunk.indexOf(c,idx+1);if(t>idx&&t<nextIdx)nextIdx=t}}const w=chunk.slice(idx,nextIdx);const nameMatch=w.match(/(?:レース|R|Ｒ)\s*([^\d]{2,40}?)(?:\s+[234]歳|サラ系|芝|ダ|障|オープン|未勝利|新馬|１勝|1勝|２勝|2勝|３勝|3勝|G)/);const distMatch=w.match(/(芝|ダート|ダ|障害|障)\s*([0-9]{3,4})/);const headMatch=w.match(/([0-9]{1,2})頭/);const gradeMatch=w.match(/G[ⅠⅡⅢI]+|G1|G2|G3|リステッド|L|オープン|3勝|３勝|２勝|2勝|１勝|1勝|未勝利|新馬/);const condMatch=w.match(/(定量|別定|ハンデ|馬齢)/);const ageMatch=w.match(/(2歳|２歳|3歳|３歳|4歳|４歳|3歳以上|３歳以上|4歳以上|４歳以上)/);let raceName=(nameMatch&&nameMatch[1]?nameMatch[1]:"").trim();if(!raceName||raceName.length>35)raceName=`${no}R`;races.push({race:{date:normalizeDate(date),place,raceNo:String(no),raceName,grade:gradeMatch?gradeMatch[0]:"",condition:condMatch?condMatch[0]:"",surface:distMatch?(distMatch[1].replace("ダート","ダ")):"",distance:distMatch?`${distMatch[2]}m`:"",headcount:headMatch?headMatch[1]:"",age:ageMatch?ageMatch[1]:""},horses:[]})}}return races}
function fallbackRaces(date,placeFilter=""){const places=placeFilter?[placeFilter]:["東京","京都","新潟"];const names=["1勝クラス","1勝クラス","2勝クラス","2勝クラス","特別","2勝クラス","3勝クラス","特別","3勝クラス","特別","メインレース","2勝クラス"];return places.flatMap(place=>Array.from({length:12},(_,i)=>({race:{date:normalizeDate(date),place,raceNo:String(i+1),raceName:names[i],grade:i===10?"OP":(i>=8?"3勝":(i>=2?"2勝":"1勝")),condition:i===10?"別定":"定量",surface:i%3===0?"ダ":"芝",distance:i%3===0?"1800m":"1600m",headcount:"",age:"3歳以上"},horses:[]})))}
async function addEntryHorses(races){return races.map(r=>{const n=Number(r.race.headcount||0);if(!r.horses||r.horses.length===0)r.horses=Array.from({length:n||18},(_,i)=>({frame:String(Math.min(8,Math.ceil((i+1)/2))),no:String(i+1),name:"",last1:"",last2:"",last3:"",odds:""}));return r})}
async function getSchedule(req){const url=new URL(req.url);const place=url.searchParams.get("place")||"";let dates=[];const date=url.searchParams.get("date");if(date)dates=[normalizeDate(date)];else dates=thisWeekend();let all=[],usedSource="jra_calendar";for(const d of dates){try{const got=await calendarRaces(d);all.push(...got)}catch(e){usedSource="fallback_calendar";all.push(...fallbackRaces(d,place))}}if(place)all=all.filter(r=>r.race.place.includes(place));const beforeFilter=all.length;all=filterTargetRaces(all);all=await addEntryHorses(all);return json({ok:true,source:usedSource,dates,filter:"未勝利・新馬・2歳戦（重賞除く）は除外",received:beforeFilter,kept:all.length,races:all,warnings:usedSource==="fallback_calendar"?["JRAページ取得に失敗したため、枠だけのフォールバックを返しました。","保存対象外は除外済みです。"]:["保存対象外は除外済みです。"]})}
export default{async fetch(req){if(req.method==="OPTIONS")return new Response("{}",{headers});try{return await getSchedule(req)}catch(e){return json({ok:false,error:String(e),races:[]})}}};
